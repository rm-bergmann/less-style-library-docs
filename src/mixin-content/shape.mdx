---
title: Shape
slug: shape
description: Mixins to style elements as shapes
---

import CodeBlock from '../components/codeBlock/codeBlock';

### Shapes follow the following pattern:

<CodeBlock mixinName={`shape`}>
  #shape.shape-type(direction, size, color);
</CodeBlock>

* Shape-type mixins currently supported are: **arrow**, **round** and **square**
* Direction supports the following values (arrow shapes only) **up**, **down**, **left** or **right**
* Size (width and height as 1 value) in px, em, rem etc
* Color

<div className="shape--container">
  <div className="shape--arrow---left" />
</div>

<CodeBlock mixinName={`shape`}>
  #shape.arrow(left, 50px, @color-01);
</CodeBlock>

<div className="shape--container">
  <div className="shape--arrow---up-cyan" />
</div>

<CodeBlock mixinName={`shape`}>
  #shape.arrow(up, 50px, @cyan);
</CodeBlock>

<div className="shape--container">
  <div className="shape--arrow---right-red" />
</div>

<CodeBlock mixinName={`shape`}>
  #shape.arrow(right, 50px, @red);
</CodeBlock>

<div className="shape--container">
  <div className="shape--arrow---down-purple" />
</div>

<CodeBlock mixinName={`shape`}>
  #shape.arrow(down, 50px, @purple);
</CodeBlock>

For round shapes you can pass in padding, width and height (as the same value) and background-color.

<div className="shape--container">
  <div className="shape--round" />
</div>

<CodeBlock mixinName={`shape`}>
  #shape.round();
</CodeBlock>

<div className="shape--container">
  <div className="shape--round---blue" />
</div>

<CodeBlock mixinName={`shape`}>
  #shape.round(1rem, 100px, @blue);
</CodeBlock>

<div className="shape--container">
  <div className="shape--square" />
</div>

<CodeBlock mixinName={`shape`}>
  #shape.square();
</CodeBlock>


 
 

