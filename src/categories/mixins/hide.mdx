---
title: Hide
slug: hide
category: mixins
description: Mixins to easily hide elements with accessibilty in mind
---

import CodeBlock from '../../components/codeBlock/codeBlock';

<div className={`code-block__description`}>
  Use the following mixins for image replacement or hiding text:
</div>

<div className="code-block">
  <CodeBlock mixinName={`position`}>
    #hide.text();
  </CodeBlock>
</div>

<div className={`code-block__description`}>
  If you want to maintain accessibilty you can specify it like this:
</div>

<div className="code-block">
  <CodeBlock mixinName={`position`}>
    #hide.text(accessible);
  </CodeBlock>
</div>

<div className={`code-block__description`}>
  If you want to hide a HTML element use the following mixin:
</div>

<div className="code-block">
  <CodeBlock mixinName={`position`}>
    #hide.element();
  </CodeBlock>
</div>






